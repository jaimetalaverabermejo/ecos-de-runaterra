import fs from 'node:fs';
const path = 'src/scenes/BattleScene.ts';
let text = fs.readFileSync(path, 'utf8');
const a = ".setWordWrapWidth(436, true)";
const b = ".setWordWrapWidth(350, true)";
if (!text.includes(a)) throw new Error('No se encontró ancho de diálogo');
text = text.replace(a, b);
const oldPrompt = "const prompt = UiKit.button(this, 430, 205, 142, 24, 'A · CONTINUAR', done, {";
const newPrompt = "const prompt = UiKit.button(this, 452, 181, 92, 18, 'A · CONTINUAR', done, {";
if (!text.includes(oldPrompt)) throw new Error('No se encontró prompt continuar');
text = text.replace(oldPrompt, newPrompt);
fs.writeFileSync(path, text);
console.log('v15.4: continuar integrado en caja de diálogo');
